{
  "name": "bg-remover-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && esbuild public/js/app.ts --bundle --minify --outfile=public/js/app.js --format=esm",
    "dev": "vercel dev",
    "deploy": "npm run build && vercel --prod"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0",
    "vercel": "^33.0.0"
  }
}
