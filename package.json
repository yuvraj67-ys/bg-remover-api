{
  "name": "bg-remover-api",
  "version": "1.0.0",
  "description": "Free browser-based background remover API on Vercel",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.2.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": "24.x"
  }
}
